import { ActionType } from "../generated/facade";
import type { ActionDefinition, ActionExecutionContext } from "#tiangz/domains";

/** Generated MMORPG enum adapter; the Action data shape is reusable. / 生成配置枚举适配器；Action数据形状属于可复用层。 */
export { ActionType };
export type { ActionExecutionContext };
export type { ActionDefinition };
export type ActionTypeValue = ActionType;
